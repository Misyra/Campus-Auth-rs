# Claude Code
.claude/settings.local.json

# Workbuddy
.workbuddy/

# 工具产物
.reasonix/
# Rust
/target/
**/*.rs.bk

# Cargo 本地配置
.cargo/config.toml

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# 前端
frontend/node_modules/
frontend/dist/
# openapi-typescript 生成物（npm run typegen 产物，手写 types.ts 才是权威来源）
frontend/src/api/types.generated.ts

# Python Worker（__pycache__ 通配任意子目录，含 tests/）
__pycache__/
*.pyc
python_worker/.venv/
python_worker/venv/
python_worker/node_modules/
# Worker 本地缓存（pytest / uv 临时目录）
python_worker/.pytest_cache/
python_worker/.tmp-uv-cache/
python_worker/debug/
!python_worker/debug/.gitkeep
python_worker/.mypy_cache/
python_worker/.ruff_cache/
python_worker/build/
python_worker/dist/

# 运行时（应用启动时自动创建，不提交）
# 注意：必须锚定到仓库根目录（/ 前缀），否则会误伤 src/config、src/environment 等源码目录
/environment/
/config/
/data/
/tasks/active.txt
/tasks/.order.json
/tasks/scheduled/
/tasks/scripts/
/tasks/browser/
/.campus_network_auth/
/debug/
/temp/
/logs/
*.log
# 环境变量
.env
.env.bak
.env.local
*.env.local

# 旧前端源码（迁移参考，不提交）
frontend-legacy/

# 构建产物
*.exe
# 打包输出目录（build.ps1 产物，含可执行文件与资源）
/dist/
/dist-portable/

# TypeScript 增量编译缓存
*.tsbuildinfo

# 问题报告
issues-report.md

# Mimosa 扫描产物（本地审计缓存，不提交）
.mimosa/
frontend/.mimosa/

# 前端本地缓存（Vite / 覆盖率）
frontend/.vite/
frontend/coverage/

# ZCode 本地产物（会话计划与审计缓存，不提交）
.zcode/

# Campus-Auth 运行时加密密钥（本地生成，不提交）
.campus_network_auth/
tests/mock-servers/full-portal/*.log
mock_portal/*.log
tests/mock-servers/**/*.log

# AI 任务生成的页面捕获产物（运行时生成）
python_worker/captures/
